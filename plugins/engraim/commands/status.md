---
description: Show EngrAIm memory status for this workspace — counts, schema version, pending sessions, and expiring calibration overrides.
---
Call the `engraim-memory` MCP tool `status` and present the result clearly: number of facts, wiki pages, episodes/sources, sessions staged for curation, the schema version, and any calibration overrides nearing expiry. If there are staged sessions, suggest `/engraim:curate`. Keep it to a compact summary.
