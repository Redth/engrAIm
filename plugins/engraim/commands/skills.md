---
description: List EngrAIm workspace skills — pending drafts and promoted/active ones — with status.
---
Call the `engraim-memory` tool `list_skills` and present it clearly: pending drafts (staged in `.engraim/pending/skills/`, not yet active) and active skills (promoted into the project `.claude/skills/`). For each, show name, status, and one-line description. If there are pending drafts, suggest `/engraim:promote-skill`.
